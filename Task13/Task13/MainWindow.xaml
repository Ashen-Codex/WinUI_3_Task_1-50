<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Task13.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Task13"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Task13">

    <StackPanel Padding="20" Spacing="15" MaxWidth="400">
        <TextBlock Text="Прогресс" FontSize="20" FontWeight="Bold"/>

        <ProgressBar x:Name="MyProgressBar" Height="20"/>

        <TextBlock x:Name="ProgressText" Text="0%" FontSize="16"/>

        <Button x:Name="StartButton" Content="Старт" Click="OnStart"
               Padding="10,8"/>
        <Button x:Name="ResetButton" Content="Сброс" Click="OnReset"
               Padding="10,8"/>
    </StackPanel>
</Window>
